{
  "name": "deepseek-desktop",
  "version": "1.3.1",
  "description": "DeepSeek Harness 桌面版 —— 以 dsh 为内核的 Windows 桌面应用",
  "main": "src/main/index.js",
  "author": "EasyTZ",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "electron .",
    "test": "node --test \"test/*.test.js\"",
    "typecheck": "tsc -p tsconfig.json",
    "icon": "node scripts/gen-icon.mjs",
    "install-plugin": "node scripts/install-plugin.mjs",
    "prepare-kernel": "node scripts/prepare-kernel.mjs",
    "dist": "node scripts/install-plugin.mjs && node scripts/prepare-kernel.mjs && node scripts/verify-kernel.mjs && electron-builder --win && node scripts/collect-release.mjs",
    "dist:dir": "node scripts/install-plugin.mjs && node scripts/prepare-kernel.mjs && node scripts/verify-kernel.mjs && electron-builder --win --dir",
    "verify-kernel": "node scripts/verify-kernel.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "electron": "^43.4.1",
    "electron-builder": "^26.15.3",
    "sharp": "^0.35.3",
    "typescript": "^7.0.2"
  }
}
