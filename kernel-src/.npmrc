# 内核树本来是用正常 npm 语义装出来的（`npm i -g @deepseek-ai/dsh`）。仓库根
# app/.npmrc 的 legacy-peer-deps=true 是为 vendor 插件源码服务的（按 npm 3-6
# 语义忽略 peer 解析），如果内核安装继承了它，装出来的依赖树可能和现在发出去
# 的那棵不一样，而且不会有任何报错——直到用户那边某个功能坏掉。这里显式关掉，
# 不依赖「继承自哪层」的隐式规则。
legacy-peer-deps=false
