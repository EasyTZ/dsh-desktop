# 更新日志

**最新版 v1.7.7** —— 设置面板新增「更新」分区，启动提示语更友好：

- **设置弹窗顶部多了一个蓝色小叹号，左侧导航加了一个「更新」分区**：外壳自身（Desktop App）和 dsh 内核各自的更新状态现在都能在设置里直接查——有更新才可点，没有就是禁用的「已是最新」；内核那个按钮点了打开的是跟托盘菜单「检查内核更新」完全相同的更新中心窗口。
- **启动提示语换成人话**：「正在启动内核…」改成「正在启动 DeepSeek Harness…」；首次启动那句也说清楚了在装什么、大概要等多久。
- **修：README 里第一个徽章的图标不显示**：下载徽章的中文 label 没做 URL 编码，部分渲染器会因此把整张图当成加载失败。

内置内核 0.1.2-rc.1。随包插件：插件市场 v1.6.0、Git 面板 v0.5.6、终端面板 v0.2.7、余额显示 v0.6.7、在资源管理器中打开 v0.2.4。

**v1.7.6** —— 修复升级后默认插件可能缺失的问题：

- **四个默认面板会在随包版本升级时自动补装**：Git、终端、余额和“在访达中打开”曾被旧的播种账本误判为“用户已卸载”，即使升级包带来了新版也不再安装。现在同一版本中用户主动卸载仍会被尊重；但随应用升级到新版、而默认插件缺失时会重新从本地随包安装，无需联网或手动去市场逐个安装。

内置内核 0.1.2-rc.1。随包插件：插件市场 v1.5.1、Git 面板 v0.5.6、终端面板 v0.2.7、余额显示 v0.6.7、在资源管理器中打开 v0.2.4。

**最新版 v1.7.5** —— 修复 dsh 0.1.2 下的插件兼容问题，完善 macOS 更新与安装入口：

- **Git、终端和“在访达中打开”恢复正常**：dsh 0.1.2 的外部 store 方法开始依赖对象接收者，旧插件把方法作为裸函数交给 React，点击入口时会直接报错。三个插件现已同时兼容 0.1.1 与 0.1.2，并增加对应冒烟测试。
- **余额统计兼容 dsh 0.1.2**：会话快照尚未带 `nodes` 时不再报错，模型目录和会话列表 store 同样保留对象接收者。
- **macOS 不再依赖唯一的状态栏入口**：状态栏图标被菜单栏空间挤掉时，可从顶部应用菜单或 Dock 右键菜单打开内核更新；更新完成后三处版本状态一起刷新。
- **DMG 自带首次启动图文引导**：挂载后直接展示“拖入 Applications”及“系统设置 → 隐私与安全 → 仍要打开”步骤。当前仍是免费的 ad-hoc 签名，不冒充 Developer ID 或 Apple 公证版本。
- **README 重新排版**：桌面端插件展示从比例失衡的四宫格改成四个完整功能区；插件市场、Git、终端和余额仓库统一首屏层级、状态徽章与截图尺寸。
- **内置内核升级到 0.1.2-rc.1**：发布包与本次 macOS 兼容验证使用同一版本，不再让开发态旧内核掩盖兼容问题。

内置内核 0.1.2-rc.1。随包插件：插件市场 v1.5.1、Git 面板 v0.5.6、终端面板 v0.2.7、余额显示 v0.6.7、在资源管理器中打开 v0.2.4。

**v1.7.4** —— 支持 macOS（Apple Silicon），随包插件全部升到最新：

- **新增 macOS 支持**，出 Apple Silicon（arm64）的 dmg。Intel Mac 暂不支持。
  > **首次打开要右键 →「打开」**，之后就正常了。原因是本程序没有购买 Apple 的开发者签名（99 美元/年），系统会先拦一下。这不影响使用，也不代表程序有问题。
- **插件市场 v1.5.0**：「已安装」里加了一键更新，会串行把整批插件更完，市场自己排在最后更（免得更到一半把自己换掉）。
- **Git 面板 v0.5.5**：历史区自动铺满面板剩余空间，最后一块不再留多余的下边距。
- **终端面板 v0.2.6**：打开面板时会定位到触发呼吸灯的那个标签，不再另开一个；stderr 不再整行标红，改成区分 error / warning；徽标区分成功与失败。
- **余额显示 v0.6.6**：面板最大高度改成贴窗口顶部，内容全展开时不再多滚一屏。
- 修：全局快捷键被别的程序占用时会在日志里说明。以前注册失败是悄无声息的，用户只会觉得「这个快捷键就是没用」，无从排查。

内置内核 0.1.1-rc.2。随包插件：插件市场 v1.5.0、Git 面板 v0.5.5、终端面板 v0.2.6、余额显示 v0.6.6、在资源管理器中打开 v0.2.3。

**v1.7.3** —— 支持 Linux（AppImage），发版改走 CI，顺带修掉三个只在非 Windows 上才会露出来的老问题：


- **新增 Linux 支持**，出 x64 的 AppImage，下载 `chmod +x` 就能跑，不用安装。自带完整运行环境这条对 Linux 一样成立——包里带着内核和它自己的 node，不依赖系统装了什么。
- **发版改走 GitHub Actions**：推一个 tag，Windows 与 Linux 两个平台并行打包、各自跑完整的内核启动自检，产物汇合后一次性发布。以前是在开发者本机手动打包，只能出当前这台机器的平台。
- **内核安装方式重做**：以前是把打包机上全局装的那份 dsh 整个拷进包里——装的是哪个平台的，产物就只能是哪个平台的，而且同一份代码在不同时间打包可能装进不同版本的内核。现在按仓库里声明的精确版本干净安装，版本跟着代码走，也才谈得上给别的平台出包。
- **修：内核解包时不保留可执行权限**。这是个一直都在、但只有非 Windows 才会发作的问题——Windows 上没有「可执行位」这回事，所以从没暴露过；换到 Linux，走到兜底解包路径时解出来的 node 直接是不可执行的，内核起不来。
- **修：退出应用后可能残留内核进程**。之前只杀直接子进程，内核自己拉起的终端 shell 会变成孤儿、占着端口不放；现在按进程组整棵树一起收。另外应用崩溃或被强杀时留下的内核，下次启动会自动清掉（会先核对确实是我们的进程才动手，不会误杀）。
- **修：Linux 上没有系统托盘时应用起不来**。部分桌面环境不带 libappindicator，创建托盘会直接抛异常；现在降级为无托盘运行，其余功能不受影响。

内置内核 0.1.1-rc.2。随包插件：插件市场 v1.4.0、Git 面板 v0.5.4、终端面板 v0.2.4、余额显示 v0.6.4、在资源管理器中打开 v0.2.3。

**v1.7.2** —— 随包插件全部升到最新，插件市场「发现」页更顺手：

- **插件市场「发现」页按屏懒加载**，卡片高度钉死不再让页面跳动；图片清单落盘缓存；`/preview` 缩略图带版本号后不再取不到图。
- 其余四个随包插件（Git 面板、终端面板、余额显示、在资源管理器中打开）只是 README/简介与截图更新，截图现在会进 tarball,插件市场才取得到缩略图。
- 打包脚本内部改进:插件工作副本跟钉住的 tag 对不上这道核对,以前只在联调态生效,现在非联调态也会核对(只警告不中止),避免插件改了、tag 没打、产物却悄悄用了旧版本;打包完只保留当前版本的产物,不再让 `dist/` 越攒越大。

内置内核 0.1.1-rc.2。随包插件:插件市场 v1.4.0、Git 面板 v0.5.4、终端面板 v0.2.4、余额显示 v0.6.4、在资源管理器中打开 v0.2.3。

**v1.7.1** —— 插件有新版本时侧边栏直接告诉你，运行时升级到 Electron 44：

- **已安装的插件有新版本时，侧边栏「插件市场」四个字的右上角会出现一个蓝色小叹号**（侧边栏折叠时挂在图标上）。不用再时不时点开市场、翻一遍「已安装」列表才知道有没有更新。点完「更新到 x.y.z」叹号立刻消失。查不到的时候（离线、装插件的目录找不到）保持上一次的结果，不会假装「没有更新」。
- **插件市场面板里本该是蓝色的地方，之前全是白的**。链接、搜索框聚焦时的边框、「dsh 插件」徽章、开关的选中色——用的那个主题色在这套配色里实际是近白色，现在都改成蓝色了。
- **运行时升级到 Electron 44**（原先是 43），依赖里的两个安全告警（一高一中）一并清掉。
- **余额插件补上一条防线**：写「本日/周/月花费」那个接口的 POST 现在要求 `Content-Type: application/json`。它此前只查了来源，而来源检查挡不住用 `text/plain` 发出来的跨源请求——这两条是配套的，只有一条等于没有。
- 修了一个只在开发者机器上出现的问题：源码联调时，启动对账会把插件市场的联调链接换成随包的旧版本，表现是「改完代码怎么都不生效」，而日志里只有一句看起来毫不相干的 pnpm 报错。

内置内核 0.1.1-rc.2。随包插件：插件市场 v1.3.0、Git 面板 v0.5.3、终端面板 v0.2.3、余额显示 v0.6.1、在资源管理器中打开 v0.2.2。

**v1.6.4** —— 修掉一个会让应用彻底打不开的插件安装故障，插件市场在 Linux/WSL 上终于能用：

- **装到一个「发布不完整」的插件不会再让应用起不来**。npm 上有些插件声明了自己要用的配置文件，实际发布时却漏带了那个文件；装上之后下次启动内核直接报错退出，反复弹错，连安全模式都进不去——只能手动去删目录。现在两道关：市场装完会当场核对那个文件在不在，不在就自动把这次安装撤销掉、告诉你这个包发布得不完整；万一还是漏进去了，启动时的自查也会把它摘掉，应用照常打开。
- **Linux / macOS / WSL 上插件市场的安装、卸载按钮不再是灰的**。此前只有 Windows 能用，其他系统上市场只能看不能装——定位 dsh 命令的方式在那些系统上认不出来（那里的全局命令是个符号链接）。
- **卸载插件只压暗被卸的那一张卡片**。以前卸掉一个，整个市场面板都变灰、按钮全禁用，想卸三个就得重启三次。现在可以连着卸好几个，攒够了一次重启。顺带：卸载后卡片不会再跳到列表末尾，徽章文案简化成「已卸载」。
- **在「发现」里装插件不会再把列表滚回顶部**，从插件详情返回也会回到原来的位置——以前翻了几屏之后装一个包，位置全丢，得从头再滚一遍。
- **搜索不会再动不动报「HTTP 429」**。市场原先每 10 分钟、以及每次重启后都要把 npm 上 3600 多个 dsh 插件重抓一遍（15 次请求），很容易被限流挡下。现在索引缓存 6 小时并落盘，过期时先用旧的、后台悄悄刷新；顶部刷新按钮可以随时强制重抓。
- **侧边栏底部的插件入口不会再挤在同一行**。这个问题只在没装终端面板插件的环境里出现（比如自己用 `dsh plugin add` 装了市场和余额的用户），三个图标挤成一行，余额那行字被压得只剩省略号。
- **在浏览器里直连 dsh 时，「重启后生效」那句话会说清要重启什么**。网页版没有「重启应用」按钮（那是桌面版才有的），以前横幅只写「重启后生效」，看上去像是按钮坏了；现在明说「重启 dsh 进程后生效」。
- **修了余额面板的花费清零**：中途某次请求报错时，之前累计的花费会被抹掉；现在保留流式估算的结果，并把累计值持久化，刷新页面也不会归零。

内置内核 0.1.1-rc.2。随包插件：插件市场 v1.2.1、Git 面板 v0.5.2、终端面板 v0.2.2、余额显示 v0.5.2、在资源管理器中打开 v0.2.1。

**v1.6.3** —— 随包的余额插件升到 v0.5.0：

- **侧边栏那一行现在同时显示余额和本次打开花费**：以前只有余额，花费得点开面板才看得到。还没花钱时显示 0，不是「—」。
- **生成过程中花费实时跳动**：以前要等一条消息完全结束才更新，现在按已经输出的内容估算，消息结束时用精确用量校正。
- **「目前单价」加上了币种**：以前只有几个裸数字，是人民币还是美元全靠猜。顺带修了单价偶尔显示成 `0.9099999999999999` 这种一长串小数的问题。

**v1.6.2** —— 随包的插件市场升到 v1.1.0、余额插件升到 v0.4.1：

- **插件市场「发现」页新增国内镜像开关**：打开后搜索、详情、README 抓取和安装都改走 `registry.npmmirror.com`，下载量也自动切换数据源；默认关闭，用哪个源由你自己决定。
- **修了市场搜索偶尔搜不到的问题**：不再把 `keywords:dsh-plugin` 和你的搜索词拼在一起交给 npm——之前搜 `easytz` 这类词会被这条限定语法带偏；现在「只看 dsh 插件」模式下先抓全量再本地过滤，自由词搜索更准。
- **发现页卡片能直接安装了**：每张卡片上直接有「详情」「安装」两个按钮，不用先展开。
- **余额插件修了两个计费问题**：模型目录晚于首报加载时，同一条消息会从「未计价」迁移到真实模型并补记费用；单价拉取失败后不会再把 `null` 永久缓存，面板之后还有机会重试。
- **更新/安装/卸载插件后不再各弹一个「重启应用」按钮**：这些操作和停用/启用插件一样，统一并到面板底部那一条共用的「重启后生效」提示里，不用在好几张卡片之间找该点哪一个。
- **插件市场自己现在也能卸载了**：跟 Git 面板、终端这些自带插件一样对待——卸载后下次重启会自动重新装回来（它是随包分发的必备插件，不需要你手动去装），不用再担心手滑卸掉就再也找不到插件管理入口。但「停用」还是锁着的，因为停用没有自动恢复机制，锁着能避免真的把自己关在门外。

**v1.6.1** —— 随包的余额插件升到 v0.4.0：

- **余额面板改版**：侧边栏入口从每条回复下面挪到了插件市场入口下方，展开态是一行字，折叠态跟 Git / 终端 / 市场一样只显示图标；点开是详情面板，能看用量、预估花费、当前单价，点空白处或按 Esc 都能关闭。
- **按当前选中的模型分开算花费**：单价改成按会话当前选中的 DeepSeek 模型（deepseek-v4-flash / pro / flash-vision-exp）分别折算，不再是不区分模型的单一价格；用了官方定价页当前的真实单价，还加了峰谷动态定价（北京时间工作日 9-12、14-18 点按 2 倍单价算）。没配置单价的模型只显示用量、不计入花费。

**v1.6.0**：

- **应用有新版本会提醒你**：启动时自动查一下 GitHub 上的最新发布，有新版就弹系统通知、托盘菜单里也会常驻一项，点开就是发布页。不会自动下载安装——安装版和绿色版都需要你自己去下，这里只负责让你知道有新版可用。
- **插件市场修了几个卡片视图的问题**：排序下拉框换成自己画的，不再是样式突兀的系统原生下拉；修了一个「快速切换排序时，卡片有时不按新排序显示、也不显示下载量」的偶发问题；插件开关来回点几次不再被误算成好几项「待重启生效」的改动；点开关的响应速度和后面按钮的位置也顺手修了。
- 安全模式提示里过时的「设置 → 插件」文案改成了正确的入口（插件市场的「已安装」标签页）。

**v1.5.1** —— 修掉一个会让应用起不来的插件卸载故障：

- **卸载插件失败后不会再让应用打不开**。此前如果卸载中途出错（包已经删了、清单里的声明还在），下次启动时内核会因为找不到那个插件而直接退出，反复弹错，连安全模式都进不去。现在启动时会先自查一遍：清单里声明了、但实际已经不在的插件会被自动摘掉，应用照常启动，你可以在市场里把它装回来或者重试卸载。
- **插件的安装包改放到你自己的配置目录里**。此前自带插件的安装包路径指向应用的安装目录，应用一升级里面的文件就换了名字，导致之后**任何**插件都装不上也卸不掉——哪怕你操作的是另一个毫不相干的插件。现在这些安装包会复制一份到 `~/.dsh` 下，不随应用升级或卸载而失效；已经记着旧路径的会在启动时自动改过来。

**v1.5.0** —— 插件体系的一次重做，用户能直接看到的是**插件市场**：

- **插件市场**：侧边栏底部新增入口（在 Git 之下）。可以搜 npm 上的 dsh 插件、看详情与截图、一键安装，不用碰命令行；已安装的插件在这里统一停用 / 启用 / 卸载。原来「设置 → 插件 → 桌面插件」那个页面已经并进市场，设置里不再单列。
- **自带的插件也能卸载了**：Git 面板、终端、余额显示、在资源管理器中打开这四个，现在和你从市场装的插件是**完全一样**的管理方式 —— 可以卸掉，卸掉之后市场里还能一键装回来（用安装包里自带的副本，不需要联网）。只有插件市场自己卸不掉，因为它是管理入口，删了就再也装不回来。
- **升级内核不再重装插件**：插件搬到了你自己的 dsh 配置目录里，和内核彻底分开。内核热更新之后插件原样还在，也不会再出现「更新完某个插件没跟上」这类问题。
- 插件全部改名到 `@easytz/` 命名空间并发布到 npm，其他 dsh 用户可以直接 `dsh plugin add @easytz/dsh-git` 装到自己的环境里，不必用桌面版。

**v1.4.1** —— 一次以**稳固性**为主的维护更新，日常使用看不出变化，但少了几类会在特定情况下咬人的问题：

- **插件的本地接口加了跨源防护**：Git 面板、终端、余额、「在资源管理器中打开」这几个功能是通过本机回环端口通信的，现在会拒绝来自外部网页的请求。此前浏览器里打开的恶意页面理论上可以隔空触发提交、推送或撤销改动。
- **内核更新更保守**：更新时如果有任何一个插件没装成功，整次更新直接失败并继续用当前内核，而不是装一半就启用。以前那种情况下，出问题的插件如果恰好是被你关掉的，故障会推迟到你哪天重新打开它才爆发。
- 修掉内核更新在部分机器上（装了 Hyper-V / WSL2 / Docker）可能一直失败的问题，与 v1.3.1 修的是同一类端口占用。
- 其余是开发侧的加固：打包会核对内核版本、发布前校验插件版本钉得是否正确、插件仓库补上单元测试与 CI。

**v1.4.0** —— 这一版主要围绕**插件**：

- **插件管理**：设置 → 插件 → 桌面插件，自带的几个插件可以逐个开关（重启后生效）。
- **安全模式**：万一某个插件把内核搞崩，崩溃提示框上多了「安全模式启动」——只加载插件管理面板，进去把出问题的插件关掉就能恢复正常启动。以前遇到这种情况只能重装。
- **Git 提交详情**：点击提交历史里的任意一条，可以看到完整的提交信息（以前只显示第一行）、这次改了哪些文件、每个文件各增删多少行；重命名会显示「旧路径 → 新路径」。按 Esc、点右上角 × 或点空白处关闭。
- **配色修正**：Git 面板与终端面板的背板之前是浮在界面上的一块中灰色，现在改成和左侧菜单一个底色；输入框、下拉菜单、终端标签页也换成配套的颜色，深浅两个主题下都是干净的对比。
- 顺手修掉一类潜在的启动失败（自带插件的内部标识可能与内核内置条目重名）。

另外，Git 面板、终端面板、余额显示、在资源管理器中打开这四个插件已拆成独立开源仓库（GitHub 上打了 `dsh-plugin` 标签），其他 dsh 用户也能装到自己的环境里。

**v1.3.3** —— Git 面板与终端的一轮打磨：修掉深浅主题下按钮、标签「和背景糊在一起」的问题；提交历史用绿/黄圆点区分已推送与未推送，更早的提交折叠起来（带展开动画）；打开工作区时终端自动开一个对应标签，当前路径高亮显示。

**v1.3.2** —— 绿色版解压从三分多钟降到 5 秒（内核在包里改成单个文件，首次启动时自动展开，约 8 秒）；Git 面板按反馈改版：提交与推送拆成两个独立按钮、提交框移到分支下方、每条提交标出已推送 / 未推送、分支下拉放大、变更列表可折叠。

**v1.3.1** —— 修复部分机器上「dsh 内核启动失败」（端口被系统保留区间占用，Hyper-V / WSL2 / Docker 会动态预留大段端口）；安装包与绿色版体积各减约 30 MB，绿色版少了 1.4 万个文件，解压明显更快。

**v1.3.0** —— 侧边栏新增 **Git 面板**（改动文件、暂存、提交、推送、切分支、撤销最新提交）与 **终端面板**（当前工作区执行命令、实时输出、多标签、Tab 补全、Ctrl+C 终止）；会话标题栏新增「在资源管理器中打开」；标题栏收窄并修掉与侧边栏交界处的色差。
